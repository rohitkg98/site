## Serving Templates

- base

> base template is in `base.html`.
> will contain all the content which is uniform across the site.
> it will refer 2 more templates: `title` and `content`.


- title

> contains title of that page.
> every page defines a title template.


- content

> contains body of that page.
> every page defines a content template.